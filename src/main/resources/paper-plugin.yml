name: Vessel
version: ${version}
main: org.maboroshi.vessel.Vessel
loader: org.maboroshi.vessel.Loader
description: Redefine mob transportation on your server. Safely catch, store, and release any entity using highly customizable pocket items.
author: Maboroshi
website: https://github.com/MaboroshiKobo/Vessel
api-version: 26.1
folia-supported: true
modrinth-project-id: vessel
hangar-project-slug: vessel
github-repo: MaboroshiKobo/Vessel
dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
    Nexo:
      load: BEFORE
      required: false
    MythicMobs:
      load: BEFORE
      required: false
    WorldGuard:
      load: BEFORE
      required: false
    Towny:
      load: BEFORE
      required: false
    GriefPrevention:
      load: BEFORE
      required: false